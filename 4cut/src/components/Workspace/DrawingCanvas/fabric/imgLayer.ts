import * as fabric from 'fabric';

// Img 타입 레이어를 위한 함수 (구현은 추후)
export function syncImgLayer(
  canvas: fabric.Canvas,
  imgData: any, // 실제 타입으로 교체 필요
  options?: any // 필요시 추가 옵션
) {
  // TODO: Img 타입 그리기 구현
} 