
import PhotoCapture from "../components/PhotoShoot/PhotoCapture/PhotoCapture.tsx";
import "../styles/PhotoShoot/PhotoShootPage.css";

function PhotoShootPage() {
  return (
      //<PhotoCapture aspectRatio={4 / 3} />
      <PhotoCapture />
  );
}

export default PhotoShootPage;